---
title: Lean register
description: DOI-grade Lean register. Handle means proven by decide for fourteen observers. Mint empty.
outline: deep
---

# Lean register <Badge type="warning" text="mint empty" />

Product `{{ reading.product }}`. Holds `{{ reading.holds }}`. Host `{{ reading.host }}`. DOI `{{ reading.doi.id }}` sealed. Redirect `{{ reading.doi.redirect }}` verified `{{ reading.doi.verified }}`.

Only a theorem published in Zenodo as a publication may be shared elsewhere. Open leads on prove2.me are not Zenodo publications and are not shared.

Open leads stay on [prove2.me](https://prove2.me/). Status `Open`. Captain name is not on an open theorem.

Sealed standing keys post prior art first, then captain `{{ reading.doi.captain }}` with DOI `{{ reading.doi.id }}`. This worker does not mint keys.

::: info JSON
`curl -sS https://lean.uuidna.com/register` · alias [Publications](/publications)
:::
