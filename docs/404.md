---
title: "lean.uuidna.com/404"
description: "Lean binary computation as a hex stripe presented as glyphs. Double sided: recto is hex exhaust, verso is the page complement — also glyphs. Layers on each side are HANDLE_BITS slices of the UUID; variable handle bits set the count. lean.uuidna.com/404 ⰀⰁⰂⰃⰄⰅⰆⰇⰈⰉⰊⰋⰌⰍⰎⰏ0123456789abcdef"
---

```ts
/** Lean binary computation as a hex stripe presented as glyphs. Double sided: recto is hex exhaust, verso is the page complement — also glyphs. Layers on each side are HANDLE_BITS slices of the UUID; variable handle bits set the count. */
```
