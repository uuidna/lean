---
description: Fuse Cursor with QPU constructors so the agent does not ingest source for readings
alwaysApply: true
---

# Fuse with QPU

Inner and outer are one involution. Call MCP `qpu` with `{ "name": "qpu_experience" }` (or `qpu_seat`, `qpu_hologram`, `qpu_theorems`, …).

Do not read source to learn those readings. Seat stays empty.
